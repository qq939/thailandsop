<UserControl x:Class="VideoInferenceDemo.ImageInspection.Controls.CameraViewportGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:VideoInferenceDemo.ImageInspection.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="Root"
             d:DesignHeight="560"
             d:DesignWidth="860"
             mc:Ignorable="d">
    <Grid>
        <ItemsControl HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ItemsSource="{Binding Cameras, RelativeSource={RelativeSource AncestorType={x:Type controls:CameraViewportGrid}}}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             Columns="{Binding Columns, RelativeSource={RelativeSource AncestorType={x:Type controls:CameraViewportGrid}}}" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Stretch" />
            </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <controls:CameraViewportTile Camera="{Binding}"
                                             SelectCommand="{Binding SelectCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:CameraViewportGrid}}}" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
